// Fonte bitmap 5x7 desenhada em código. Acentos são compostos em tempo de
// desenho a partir da decomposição Unicode (NFD), evitando um glifo por acento.
(function (global) {
  const G = {
    A: [0x0e, 0x11, 0x11, 0x1f, 0x11, 0x11, 0x11],
    B: [0x1e, 0x11, 0x11, 0x1e, 0x11, 0x11, 0x1e],
    C: [0x0e, 0x11, 0x10, 0x10, 0x10, 0x11, 0x0e],
    D: [0x1e, 0x11, 0x11, 0x11, 0x11, 0x11, 0x1e],
    E: [0x1f, 0x10, 0x10, 0x1e, 0x10, 0x10, 0x1f],
    F: [0x1f, 0x10, 0x10, 0x1e, 0x10, 0x10, 0x10],
    G: [0x0e, 0x11, 0x10, 0x17, 0x11, 0x11, 0x0f],
    H: [0x11, 0x11, 0x11, 0x1f, 0x11, 0x11, 0x11],
    I: [0x0e, 0x04, 0x04, 0x04, 0x04, 0x04, 0x0e],
    J: [0x07, 0x02, 0x02, 0x02, 0x02, 0x12, 0x0c],
    K: [0x11, 0x12, 0x14, 0x18, 0x14, 0x12, 0x11],
    L: [0x10, 0x10, 0x10, 0x10, 0x10, 0x10, 0x1f],
    M: [0x11, 0x1b, 0x15, 0x15, 0x11, 0x11, 0x11],
    N: [0x11, 0x19, 0x15, 0x13, 0x11, 0x11, 0x11],
    O: [0x0e, 0x11, 0x11, 0x11, 0x11, 0x11, 0x0e],
    P: [0x1e, 0x11, 0x11, 0x1e, 0x10, 0x10, 0x10],
    Q: [0x0e, 0x11, 0x11, 0x11, 0x15, 0x12, 0x0d],
    R: [0x1e, 0x11, 0x11, 0x1e, 0x14, 0x12, 0x11],
    S: [0x0f, 0x10, 0x10, 0x0e, 0x01, 0x11, 0x0e],
    T: [0x1f, 0x04, 0x04, 0x04, 0x04, 0x04, 0x04],
    U: [0x11, 0x11, 0x11, 0x11, 0x11, 0x11, 0x0e],
    V: [0x11, 0x11, 0x11, 0x11, 0x11, 0x0a, 0x04],
    W: [0x11, 0x11, 0x11, 0x15, 0x15, 0x1b, 0x11],
    X: [0x11, 0x11, 0x0a, 0x04, 0x0a, 0x11, 0x11],
    Y: [0x11, 0x11, 0x0a, 0x04, 0x04, 0x04, 0x04],
    Z: [0x1f, 0x01, 0x02, 0x04, 0x08, 0x10, 0x1f],
    0: [0x0e, 0x11, 0x13, 0x15, 0x19, 0x11, 0x0e],
    1: [0x04, 0x0c, 0x04, 0x04, 0x04, 0x04, 0x0e],
    2: [0x0e, 0x11, 0x01, 0x02, 0x04, 0x08, 0x1f],
    3: [0x1f, 0x02, 0x04, 0x02, 0x01, 0x11, 0x0e],
    4: [0x02, 0x06, 0x0a, 0x12, 0x1f, 0x02, 0x02],
    5: [0x1f, 0x10, 0x1e, 0x01, 0x01, 0x11, 0x0e],
    6: [0x06, 0x08, 0x10, 0x1e, 0x11, 0x11, 0x0e],
    7: [0x1f, 0x01, 0x02, 0x04, 0x08, 0x08, 0x08],
    8: [0x0e, 0x11, 0x11, 0x0e, 0x11, 0x11, 0x0e],
    9: [0x0e, 0x11, 0x11, 0x0f, 0x01, 0x02, 0x0c],
    ".": [0, 0, 0, 0, 0, 0, 0x04],
    ",": [0, 0, 0, 0, 0, 0x04, 0x08],
    "!": [0x04, 0x04, 0x04, 0x04, 0x04, 0, 0x04],
    "?": [0x0e, 0x11, 0x01, 0x02, 0x04, 0, 0x04],
    "-": [0, 0, 0, 0x0e, 0, 0, 0],
    "/": [0x01, 0x02, 0x02, 0x04, 0x08, 0x08, 0x10],
    ":": [0, 0x04, 0, 0, 0, 0x04, 0],
    "'": [0x04, 0x04, 0, 0, 0, 0, 0],
    "(": [0x02, 0x04, 0x08, 0x08, 0x08, 0x04, 0x02],
    ")": [0x08, 0x04, 0x02, 0x02, 0x02, 0x04, 0x08],
    "%": [0x11, 0x01, 0x02, 0x04, 0x08, 0x10, 0x11],
    "+": [0, 0x04, 0x04, 0x1f, 0x04, 0x04, 0],
    "*": [0, 0x15, 0x0e, 0x1f, 0x0e, 0x15, 0],
    "=": [0, 0, 0x1f, 0, 0x1f, 0, 0],
    "<": [0x02, 0x04, 0x08, 0x10, 0x08, 0x04, 0x02],
    ">": [0x08, 0x04, 0x02, 0x01, 0x02, 0x04, 0x08],
    "#": [0x0a, 0x1f, 0x0a, 0x0a, 0x1f, 0x0a, 0],
    "~": [0, 0, 0x0d, 0x16, 0, 0, 0],
    "×": [0, 0x11, 0x0a, 0x04, 0x0a, 0x11, 0],
  };

  // Marcas combinantes (NFD) desenhadas acima do glifo.
  const ACCENT_ABOVE = {
    "́": [0x03, 0x06], // agudo
    "̀": [0x0c, 0x06], // grave
    "̂": [0x04, 0x0a], // circunflexo
    "̃": [0x0d, 0x16], // til
    "̈": [0x0a, 0x00], // trema
  };
  const ACCENT_BELOW = {
    "̧": [0x04, 0x0c], // cedilha
  };

  const CHAR_W = 5;
  const CHAR_H = 7;
  const ADVANCE = 6;
  const ASCENT = 3; // espaço reservado acima para acentos
  const LINE_H = 12;

  function glyphFor(ch) {
    return G[ch] || null;
  }

  function decompose(text) {
    const out = [];
    const norm = String(text).toUpperCase().normalize("NFD");
    for (const ch of norm) {
      if (ACCENT_ABOVE[ch] || ACCENT_BELOW[ch]) {
        if (out.length) out[out.length - 1].marks.push(ch);
      } else {
        out.push({ base: ch, marks: [] });
      }
    }
    return out;
  }

  function measure(text) {
    return decompose(text).length * ADVANCE;
  }

  function drawRows(ctx, rows, x, y, color) {
    ctx.fillStyle = color;
    for (let r = 0; r < rows.length; r++) {
      const bits = rows[r];
      if (!bits) continue;
      for (let c = 0; c < CHAR_W; c++) {
        if (bits & (1 << (CHAR_W - 1 - c))) ctx.fillRect(x + c, y + r, 1, 1);
      }
    }
  }

  // Desenha texto com (x, y) no canto superior esquerdo da caixa de linha.
  function draw(ctx, text, x, y, color, shadow) {
    const chars = decompose(text);
    let cx = x;
    for (const { base, marks } of chars) {
      const rows = glyphFor(base);
      if (rows) {
        if (shadow) drawRows(ctx, rows, cx + 1, y + ASCENT + 1, shadow);
        drawRows(ctx, rows, cx, y + ASCENT, color);
        for (const m of marks) {
          const above = ACCENT_ABOVE[m];
          if (above) {
            if (shadow) drawRows(ctx, above, cx + 1, y + 1, shadow);
            drawRows(ctx, above, cx, y, color);
          }
          const below = ACCENT_BELOW[m];
          if (below) {
            if (shadow) drawRows(ctx, below, cx + 1, y + ASCENT + CHAR_H + 1, shadow);
            drawRows(ctx, below, cx, y + ASCENT + CHAR_H, color);
          }
        }
      }
      cx += ADVANCE;
    }
    return cx - x;
  }

  // Quebra texto em linhas que caibam em maxWidth pixels.
  function wrap(text, maxWidth) {
    const words = String(text).split(/\s+/).filter(Boolean);
    const lines = [];
    let line = "";
    for (const w of words) {
      const candidate = line ? line + " " + w : w;
      if (measure(candidate) > maxWidth && line) {
        lines.push(line);
        line = w;
      } else {
        line = candidate;
      }
    }
    if (line) lines.push(line);
    return lines;
  }

  global.Font = { draw, measure, wrap, LINE_H, CHAR_H, ADVANCE, ASCENT };
})(window);
